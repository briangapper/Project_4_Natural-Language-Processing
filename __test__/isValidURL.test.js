// ************************************************************
// ------------------------------------------------------------
// 0.) IMPORTS
// ------------------------------------------------------------
// ************************************************************
import { isValidURL } from "../src/client/js/formFunctions"

// ************************************************************
// ------------------------------------------------------------
// 1.) JEST
// ------------------------------------------------------------
// ************************************************************
describe('isValidURL', () => {

    // ************************************************************
    // ------------------------------------------------------------
    // 1.1) TESTS
    // ------------------------------------------------------------
    // ************************************************************

    // ------------------------------------------------------------
    // 1.1.1) Test 1: Return true for valid URL
    // ------------------------------------------------------------
    test('it should return true for a valid URL', () => {
        const url = 'https://example.com';
        expect(isValidURL(url)).toBe(true);
    });
  
    // ------------------------------------------------------------
    // 1.1.2) Test 2: Return false for invalid URL
    // ------------------------------------------------------------
    test('it should return false for an invalid URL', () => {
        const url = 'not a URL';
        expect(isValidURL(url)).toBe(false);
    });
    
});
  